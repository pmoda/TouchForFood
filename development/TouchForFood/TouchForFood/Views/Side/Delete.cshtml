@model TouchForFood.Models.side
@using TouchForFood.App_GlobalResources

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>side</legend>

    <div class="display-label">menu_category</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.menu_category.id)
    </div>

    <div class="display-label">name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.name)
    </div>

    <div class="display-label">price</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.price)
    </div>

    <div class="display-label">is_active</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.is_active)
    </div>

    <div class="display-label">is_deleted</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.is_deleted)
    </div>

    <div class="display-label">version</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.version)
    </div>
</fieldset>
@using (Html.BeginForm())
{
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
